{
  "version": 2,
  "buildCommand": "cd ../.. && uv pip install -r requirements.txt --system && python build.py --site sites/esign_001",
  "outputDirectory": "sites/esign_001/output",
  "installCommand": "cd ../.. && uv pip install -r requirements.txt --system",
  "framework": null,
  "routes": [
    {
      "src": "/sitemap.xml",
      "dest": "/sitemap.xml",
      "headers": { "Cache-Control": "public, max-age=86400" }
    },
    {
      "src": "/robots.txt",
      "dest": "/robots.txt",
      "headers": { "Cache-Control": "public, max-age=86400" }
    },
    {
      "src": "/(.*)",
      "dest": "/$1"
    }
  ]
}
